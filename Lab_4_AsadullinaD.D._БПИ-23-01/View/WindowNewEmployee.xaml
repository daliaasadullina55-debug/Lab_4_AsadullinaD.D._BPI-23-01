<Window x:Class="Lab_4_AsadullinaD.D._БПИ_23_01.View.WindowNewEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab_4_AsadullinaD.D._БПИ_23_01.View"
        mc:Ignorable="d"
        Title="WindowNewEmployee" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Фамилия:" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Margin="100,0,0,0" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Content="Имя:" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Margin="100,0,0,0" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Content="Дата рождения:" Grid.Row="2" VerticalAlignment="Center"/>
        <DatePicker Grid.Row="2" Margin="100,0,0,0" SelectedDate="{Binding Birthday}"/>

        <Label Content="Должность:" Grid.Row="3" VerticalAlignment="Center"/>
        <ComboBox x:Name="CbRole" Grid.Row="3" Margin="100,0,0,0" 
                  DisplayMemberPath="NameRole"
                  SelectedValuePath="Id"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="4" Margin="0,20,0,0">
            <Button Content="ОК" Width="75" Margin="5" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Отмена" Width="75" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
